#!/usr/bin/env fish

# Skip if in personal directory
# if [[ "$(pwd)" == $HOME/personal ]]; then
#     clear
#     return
# fi
if test (pwd) = "$HOME/personal"
    clear
    return
end

# Create new window with nvim
tmux new-window -d
tmux send-keys "nvim ." C-m
clear
